# 🏦 Система Управления Банковскими Картами

Система для управления банковскими картами с поддержкой аутентификации, авторизации, переводов между картами и административного управления.

## 📋 Описание

Система предоставляет функциональность для:
- Создания и управления банковскими картами
- Просмотра карт с поиском и пагинацией
- Переводов между своими картами
- Административного управления (для роли ADMIN)

## 🛠 Технологии

- **Java 17+**
- **Spring Boot 4.0.1**
- **Spring Security** с JWT аутентификацией
- **Spring Data JPA**
- **PostgreSQL 17**
- **Liquibase** для миграций БД
- **Docker & Docker Compose**
- **Swagger/OpenAPI** для документации API
- **Jasypt** для шифрования данных

## 🚀 Быстрый старт

### Предварительные требования

- Java 17 или выше
- Maven 3.6+
- Docker и Docker Compose
- Git

### Установка и запуск

1. **Клонируйте репозиторий:**
```bash
git clone <repository-url>
cd bank-card-management
```

2. **Запустите PostgreSQL через Docker Compose:**
```bash
docker-compose up -d postgres
```

3. **Проверьте, что база данных запущена:**
```bash
docker ps
```

4. **Соберите проект:**
```bash
mvn clean install
```

5. **Запустите приложение:**
```bash
mvn spring-boot:run
```

Или запустите через IDE, открыв класс `BankCardManagementApplication`.

6. **Приложение будет доступно по адресу:**
   - API: http://localhost:8081
   - Swagger UI: http://localhost:8081/swagger-ui.html
   - API Docs: http://localhost:8081/v3/api-docs

## 📚 API Документация

После запуска приложения, документация API доступна через Swagger UI:
- **Swagger UI**: http://localhost:8081/swagger-ui.html
- **OpenAPI JSON**: http://localhost:8081/v3/api-docs
- **OpenAPI YAML**: http://localhost:8081/v3/api-docs.yaml
- **OpenAPI спецификация**: `docs/openapi.yaml` (статический файл)

### Основные эндпоинты:

#### Аутентификация
- `POST /api/auth/signup` - Регистрация нового пользователя
- `POST /api/auth/signin` - Вход в систему (получение JWT токена)

#### Управление картами (USER)
- `GET /api/cards/my` - Получить список своих карт (с пагинацией и поиском)
- `GET /api/cards/my/{id}` - Получить карту по ID
- `POST /api/cards/my/{id}/block` - Запросить блокировку карты
- `POST /api/cards` - Создать новую карту

#### Переводы (USER)
- `POST /api/transfers` - Перевод между своими картами

#### Административные функции (ADMIN)
- `GET /api/admin/cards` - Получить все карты
- `GET /api/admin/cards/{id}` - Получить карту по ID
- `POST /api/admin/cards/{id}/activate` - Активировать карту
- `POST /api/admin/cards/{id}/block` - Заблокировать карту
- `DELETE /api/admin/cards/{id}` - Удалить карту
- `GET /api/admin/users` - Получить всех пользователей
- `GET /api/admin/users/{id}` - Получить пользователя по ID
- `DELETE /api/admin/users/{id}` - Удалить пользователя
- `POST /api/admin/users/{id}/block` - Заблокировать пользователя
- `POST /api/admin/users/{id}/unblock` - Разблокировать пользователя

## 🔐 Безопасность

### Роли пользователей

- **ADMIN**: Полный доступ ко всем функциям системы
- **USER**: Доступ только к своим картам и операциям

### JWT Аутентификация

После успешного входа (`/api/auth/signin`), вы получите JWT токен. Используйте его в заголовке запросов:
```
Authorization: Bearer <your-jwt-token>
```

### Шифрование данных

- Номера карт шифруются перед сохранением в базу данных
- В ответах API возвращаются только маскированные номера (формат: `**** **** **** 1234`)

## 🗄 База данных

### Структура БД

- **users** - Пользователи системы
- **roles** - Роли (ADMIN, USER)
- **user_roles** - Связь пользователей и ролей
- **bank_cards** - Банковские карты

### Миграции

Миграции выполняются автоматически при запуске приложения через Liquibase.
Файлы миграций находятся в `src/main/resources/db/changelog/`

## 🐳 Docker

### Запуск только PostgreSQL:
```bash
docker-compose up -d postgres
```

### Запуск всего стека (PostgreSQL + Backend):
```bash
docker-compose up -d
```

### Остановка:
```bash
docker-compose down
```

### Просмотр логов:
```bash
docker-compose logs -f
```

## 🧪 Тестирование

Запуск unit-тестов:
```bash
mvn test
```

## 📝 Конфигурация

Основные настройки находятся в `src/main/resources/application.yml`:

- **Порт приложения**: 8081
- **База данных**: PostgreSQL на порту 5433
- **JWT секрет**: Настраивается через `jwt.secret`
- **Шифрование**: Настраивается через `jasypt.encryptor.password`

## 🔧 Разработка

### Структура проекта

```
src/main/java/bank/card/management/
├── config/          # Конфигурации (Security, OpenAPI)
├── controller/      # REST контроллеры
├── dto/             # Data Transfer Objects
├── entity/          # JPA сущности
├── exception/       # Обработчики исключений
├── repository/      # JPA репозитории
├── security/        # JWT фильтры
├── service/         # Бизнес-логика
└── util/           # Утилиты (шифрование, маскирование)

src/main/resources/
├── db/
│   ├── changelog/  # Liquibase миграции
│   └── liquibase-changelog.yaml
└── application.yml # Конфигурация приложения
```

## 📊 Примеры использования

### 1. Регистрация пользователя

```bash
curl -X POST http://localhost:8081/api/auth/signup \
  -H "Content-Type: application/json" \
  -d '{
    "username": "user1",
    "password": "password123",
    "email": "user1@example.com",
    "firstName": "John",
    "lastName": "Doe"
  }'
```

### 2. Вход в систему

```bash
curl -X POST http://localhost:8081/api/auth/signin \
  -H "Content-Type: application/json" \
  -d '{
    "username": "user1",
    "password": "password123"
  }'
```

Ответ содержит JWT токен, который нужно использовать в последующих запросах.

### 3. Создание карты

```bash
curl -X POST http://localhost:8081/api/cards \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer <your-jwt-token>" \
  -d '{
    "cardNumber": "1234567890123456",
    "owner": "John Doe",
    "expiryDate": "2025-12-31",
    "initialBalance": 1000.00
  }'
```

### 4. Получение списка карт

```bash
curl -X GET "http://localhost:8081/api/cards/my?page=0&size=10" \
  -H "Authorization: Bearer <your-jwt-token>"
```

### 5. Перевод между картами

```bash
curl -X POST http://localhost:8081/api/transfers \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer <your-jwt-token>" \
  -d '{
    "fromCardId": 1,
    "toCardId": 2,
    "amount": 100.00
  }'
```

## 🐛 Решение проблем

### Ошибка подключения к БД

Убедитесь, что PostgreSQL контейнер запущен:
```bash
docker ps
docker-compose up -d postgres
```

### Ошибка миграций

Если миграции не применяются, проверьте логи приложения и убедитесь, что база данных доступна.

### Проблемы с JWT

Проверьте, что используете правильный формат заголовка:
```
Authorization: Bearer <token>
```

## 📄 Лицензия

Этот проект создан в образовательных целях.

## 👥 Автор

Разработано для системы управления банковскими картами.

---

**Примечание**: Для продакшн использования необходимо:
- Изменить секретные ключи (JWT secret, encryption password)
- Настроить HTTPS
- Добавить дополнительные меры безопасности
- Настроить логирование и мониторинг

